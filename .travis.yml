dist: trusty
sudo: false
notifications:
  email: change

language: rust

rust:
  - nightly

cache:
    cargo: true
    apt: true

env:
    - RUST_TEST_THREADS=1

script:
    - cd deucalion-rs
    - cargo build --verbose
    - cargo test --verbose

addons:
  apt:
    packages:
     - libsfml-dev
     - libcsfml-dev
